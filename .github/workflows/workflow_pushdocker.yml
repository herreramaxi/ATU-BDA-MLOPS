name: workflow_pushdocker
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
    publish_images:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: Printing Folders
              run:
                ls
            - name: Login to Docker
              uses: docker/login-action@v1
              with:
                username: ${{secrets.DOCKER_HUB_USERNAME}}
                password: ${{secrets.DOCKER_HUB_ACCESS_TOKEN}}

            #Comment temporarly
            # - name: build image
            #   run: 
                 
            #     docker build . -t herreramaxi/spam-modelbuild:latest

            # - name: push image to docker hub
            #   run: |
            #     docker push herreramaxi/spam-modelbuild:latest

           
            - name: Authenticate to Google Cloud
              uses: google-github-actions/auth@v1
              with:
                credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

            - name: Set up Google Cloud SDK
              uses: google-github-actions/setup-gcloud@v2
              with:
                project_id: ${{ secrets.GCP_PROJECT }}

            - name: Get GKE credentials
              run: |
                gcloud container clusters get-credentials ${{ secrets.GKE_CLUSTER }} \
                  --region ${{ secrets.GKE_REGION }} \
                  --project ${{ secrets.GCP_PROJECT }}
                # Clean up deprecated auth-provider
                kubectl config unset users.gke_${{ secrets.GCP_PROJECT }}_${{ secrets.GKE_REGION }}_${{ secrets.GKE_CLUSTER }}.auth-provider

            - name: Deploy to GKE
              env:
                USE_GKE_GCLOUD_AUTH_PLUGIN: "False"
              run: |
                kubectl apply -f kubernetes/
                kubectl rollout status deployment/spam-app